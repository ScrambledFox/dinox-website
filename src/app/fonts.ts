import { Bungee, Doppio_One } from 'next/font/google';

export const bungee = Bungee({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-bungee',
});

export const doppio = Doppio_One({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-doppio',
});
